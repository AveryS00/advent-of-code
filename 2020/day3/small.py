print(__import__('functools').reduce(lambda a, b: a*b, [__import__('functools').reduce(lambda c, d: c+d, [([__import__('math').ceil(delta_x * len(arr) / delta_y / len(arr[0])) * line for line in arr][y_pos][x_pos] == '#') for x_pos, y_pos in zip(range(0, 10000000, delta_x), range(0, len([line.rstrip('\n') for line in open('input.txt')]), delta_y)) if (arr := [line.rstrip('\n') for line in open('input.txt')])]) for delta_x, delta_y in [(1, 1), (3, 1), (5, 1), (7, 1), (1, 2)]], 1))